---
trigger: always_on
---

Use always following stack

# Frontend Layer

- Use Angular last version to build the Frontend layer
- In Angular use always standalone components
- Use signals always as possible
